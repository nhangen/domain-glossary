# Glossary Entry Template

Phase 1 will lock this format.

```markdown
### <term>

**Domain:** <Altamira | OM | Development | ...>
**Citation:** `<symbol>` in `<repo>:<path>` | `[[Obsidian/note-path]]` | `<repo>:<path>:<line>`
**Last verified:** <YYYY-MM-DD>
**Aliases:** <list, optional>

<one-paragraph definition in domain language>

<optional: counterexample / common confusion>
```
